# CodeHaus Frontend — Phase 1: Project Setup Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Stand up the CodeHaus frontend project skeleton — Vite + React + TypeScript, linting/formatting, Tailwind CSS + shadcn/ui, core runtime dependencies, a working test runner, and the full folder structure from the architecture spec — with nothing yet wired to real business logic.

**Architecture:** Follows `frontend/docs/superpowers/specs/2026-07-16-codehaus-frontend-architecture-design.md` Section 1 (Folder Structure). This plan only covers Roadmap Phase 1 (Project Setup); routing, layouts, infrastructure (API client, stores), and auth are separate plans (Roadmap Phases 3-5) — do not implement them here.

**Tech Stack:** Vite, React 18, TypeScript, ESLint (flat config) + Prettier, Tailwind CSS v4, shadcn/ui, React Router v6, TanStack Query, Zustand, Axios, Vitest + React Testing Library.

## Global Constraints

- No git repository exists in this workspace (root or `backend/`) — the user chose to skip git init for now. **No commit steps in this plan.** Each task ends with a verification step instead.
- All commands run with working directory `c:\Users\denobleza\desktop\code-refresher` unless a step says otherwise (PowerShell tool available; `cd` is passed inline per command since working directory does not persist as project root assumption).
- Package manager: npm (matches `backend/package-lock.json` convention already in this workspace).
- Do not add any dependency not listed in Tech Stack above without flagging it to the Team Lead first.

---

## File Structure

Files this plan creates or modifies, in creation order:

- `frontend/` — Vite scaffold output (package.json, tsconfig*.json, vite.config.ts, index.html, src/main.tsx, src/App.tsx, src/index.css, public/, .gitignore)
- `frontend/.prettierrc.json` — Prettier config
- `frontend/.prettierignore` — Prettier ignore list
- `frontend/eslint.config.js` — modified to add `eslint-config-prettier`
- `frontend/package.json` — modified: add `lint`, `format`, `format:check`, `test` scripts
- `frontend/src/index.css` — modified: Tailwind import + shadcn base layer
- `frontend/components.json` — shadcn/ui config (generated by CLI)
- `frontend/src/lib/utils.ts` — shadcn/ui `cn()` helper (generated by CLI)
- `frontend/src/components/ui/button.tsx`, `frontend/src/components/ui/card.tsx` — first shadcn components (generated by CLI, proves the pipeline works)
- `frontend/vitest.config.ts` — Vitest config
- `frontend/src/test/setup.ts` — jest-dom matchers setup
- `frontend/src/App.test.tsx` — smoke test
- Folder structure placeholders under `frontend/src/`: `app/`, `app/providers/`, `app/router/`, `modules/{auth,dashboard-client,dashboard-admin,projects,clients,quotations,invoices,payments,messaging,notifications,reports,settings,support}/`, `shared/{components/ui,components/common,components/feature,layouts,hooks,api,store,utils,constants,types,config}/`, `styles/`, `assets/` — each non-CLI-generated leaf folder gets a `.gitkeep` placeholder (git not yet used, but keeps the convention documented for when it is)

---

### Task 1: Scaffold Vite + React + TypeScript App

**Files:**

- Create: `frontend/` (entire Vite `react-ts` template output)

**Interfaces:**

- Produces: a running Vite dev server on `frontend/`, `npm run dev` / `npm run build` scripts available for all later tasks.

- [ ] **Step 1: Run the Vite scaffold command**

Run (from `c:\Users\denobleza\desktop\code-refresher`):

```
npm create vite@latest frontend -- --template react-ts
```

Expected: creates `frontend/` with the standard Vite `react-ts` template (package.json, tsconfig.json, tsconfig.app.json, tsconfig.node.json, vite.config.ts, index.html, src/main.tsx, src/App.tsx, src/App.css, src/index.css, public/vite.svg, .gitignore, eslint.config.js).

- [ ] **Step 2: Install dependencies**

Run:

```
cd frontend && npm install
```

Expected: exits 0, creates `frontend/node_modules` and `frontend/package-lock.json`.

- [ ] **Step 3: Verify the dev server boots**

Run:

```
cd frontend && npm run dev -- --port 5173
```

Expected output includes `Local:   http://localhost:5173/`. Confirm the process starts without error, then stop it (Ctrl+C / kill the process) — do not leave it running.

- [ ] **Step 4: Verify the production build succeeds**

Run:

```
cd frontend && npm run build
```

Expected: exits 0, prints a `dist/` output summary with no TypeScript errors.

---

### Task 2: Configure ESLint + Prettier

**Files:**

- Modify: `frontend/eslint.config.js`
- Create: `frontend/.prettierrc.json`
- Create: `frontend/.prettierignore`
- Modify: `frontend/package.json` (add scripts)

**Interfaces:**

- Consumes: `frontend/eslint.config.js` as generated by Task 1's Vite scaffold (flat config format, exports an array).
- Produces: `npm run lint`, `npm run format`, `npm run format:check` scripts usable by every later task and by CI later on.

- [ ] **Step 1: Install Prettier and the ESLint/Prettier conflict-resolution config**

Run:

```
cd frontend && npm install -D prettier eslint-config-prettier
```

Expected: exits 0, both packages appear in `package.json` devDependencies.

- [ ] **Step 2: Create the Prettier config**

Create `frontend/.prettierrc.json`:

```json
{
  "semi": true,
  "singleQuote": true,
  "trailingComma": "all",
  "printWidth": 100,
  "tabWidth": 2
}
```

- [ ] **Step 3: Create the Prettier ignore file**

Create `frontend/.prettierignore`:

```
dist
node_modules
coverage
```

- [ ] **Step 4: Wire Prettier into ESLint config**

Open `frontend/eslint.config.js`. It is a flat config exporting an array via `defineConfig`/`tseslint.config` (Vite's default template). Add `eslint-config-prettier` as the **last** entry in the array/extends chain so it disables any ESLint rule that conflicts with Prettier formatting. Add this import at the top:

```js
import eslintConfigPrettier from 'eslint-config-prettier';
```

And append `eslintConfigPrettier` as the final item in the exported config array (after the existing `tseslint.config(...)` entries).

- [ ] **Step 5: Add npm scripts**

In `frontend/package.json`, inside `"scripts"`, add:

```json
"lint": "eslint .",
"format": "prettier --write .",
"format:check": "prettier --check ."
```

- [ ] **Step 6: Verify lint and format both run clean**

Run:

```
cd frontend && npm run lint
```

Expected: exits 0, no errors on the untouched scaffold.

Run:

```
cd frontend && npm run format
```

Expected: exits 0, reformats any files that don't match the Prettier config (the fresh scaffold may reformat `package.json` or `README.md` — that's expected).

---

### Task 3: Install and Configure Tailwind CSS v4

**Files:**

- Modify: `frontend/vite.config.ts`
- Modify: `frontend/src/index.css`

**Interfaces:**

- Consumes: `frontend/vite.config.ts` as generated by Task 1 (exports `defineConfig({ plugins: [react()] })`).
- Produces: Tailwind utility classes usable in any component from Task 4 onward.

- [ ] **Step 1: Install Tailwind CSS v4 and its Vite plugin**

Run:

```
cd frontend && npm install tailwindcss @tailwindcss/vite
```

Expected: exits 0.

- [ ] **Step 2: Add the Tailwind Vite plugin**

Modify `frontend/vite.config.ts` to import and register the plugin:

```ts
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import tailwindcss from '@tailwindcss/vite';

export default defineConfig({
  plugins: [react(), tailwindcss()],
});
```

- [ ] **Step 3: Import Tailwind in the global stylesheet**

Replace the contents of `frontend/src/index.css` with:

```css
@import 'tailwindcss';
```

(Remove the Vite template's default CSS reset rules — Tailwind's preflight replaces them.)

- [ ] **Step 4: Verify Tailwind classes render**

Temporarily replace the contents of `frontend/src/App.tsx` with:

```tsx
function App() {
  return <h1 className="text-3xl font-bold text-blue-600">CodeHaus</h1>;
}

export default App;
```

Run:

```
cd frontend && npm run dev -- --port 5173
```

Open `http://localhost:5173/` (or check via `curl http://localhost:5173/` that the dev server responds 200) and confirm the page renders large bold blue text, not unstyled default text. Stop the dev server afterward. Leave `App.tsx` in this simplified state — Task 7 replaces it again.

---

### Task 4: Install and Initialize shadcn/ui

**Files:**

- Create: `frontend/components.json`
- Create: `frontend/src/lib/utils.ts`
- Create: `frontend/src/components/ui/button.tsx`
- Create: `frontend/src/components/ui/card.tsx`

**Interfaces:**

- Consumes: Tailwind CSS from Task 3 (shadcn CLI detects the existing Tailwind setup).
- Produces: `cn()` utility at `frontend/src/lib/utils.ts` and a `Button`/`Card` component pair that later tasks (and Section 5's `shared/components/ui/` promotion) build on.

- [ ] **Step 1: Run the shadcn/ui init command**

Run:

```
cd frontend && npx shadcn@latest init
```

When prompted, accept defaults for: base color (Neutral), CSS variables (Yes). Expected: creates `frontend/components.json` and `frontend/src/lib/utils.ts`, and adds a `@layer base`/CSS variable block to `frontend/src/index.css`.

- [ ] **Step 2: Add the Button and Card primitives**

Run:

```
cd frontend && npx shadcn@latest add button card
```

Expected: creates `frontend/src/components/ui/button.tsx` and `frontend/src/components/ui/card.tsx`.

- [ ] **Step 3: Verify the components compile and render**

Temporarily add to `frontend/src/App.tsx`:

```tsx
import { Button } from './components/ui/button';

function App() {
  return <Button>Click me</Button>;
}

export default App;
```

Run:

```
cd frontend && npm run build
```

Expected: exits 0 with no TypeScript errors. Revert `App.tsx` is not required yet — Task 7 replaces it.

---

### Task 5: Install Core Runtime Dependencies

**Files:**

- Modify: `frontend/package.json` (dependencies only, no code yet)

**Interfaces:**

- Produces: `react-router-dom`, `@tanstack/react-query`, `zustand`, `axios` available as imports for every later plan (Phases 3-5).

- [ ] **Step 1: Install the runtime dependencies**

Run:

```
cd frontend && npm install react-router-dom @tanstack/react-query zustand axios
```

Expected: exits 0, all four packages appear in `package.json` dependencies.

- [ ] **Step 2: Verify the build still succeeds with the new dependencies present**

Run:

```
cd frontend && npm run build
```

Expected: exits 0 (nothing imports these packages yet, so this only confirms no install-time breakage).

---

### Task 6: Set Up Vitest + React Testing Library with a Smoke Test

**Files:**

- Create: `frontend/vitest.config.ts`
- Create: `frontend/src/test/setup.ts`
- Create: `frontend/src/App.test.tsx`
- Modify: `frontend/package.json` (add `test` script)
- Modify: `frontend/src/App.tsx` (restore to a minimal real component for the smoke test to target)

**Interfaces:**

- Consumes: `frontend/src/App.tsx` default export (function component, no props).
- Produces: `npm test` script and `frontend/src/test/setup.ts` (jest-dom matchers) that every future component test in this codebase imports via Vitest's global setup.

- [ ] **Step 1: Install test dependencies**

Run:

```
cd frontend && npm install -D vitest jsdom @testing-library/react @testing-library/jest-dom
```

Expected: exits 0.

- [ ] **Step 2: Restore App.tsx to a minimal real component**

Replace `frontend/src/App.tsx` with:

```tsx
function App() {
  return (
    <main>
      <h1 className="text-3xl font-bold text-blue-600">CodeHaus</h1>
    </main>
  );
}

export default App;
```

- [ ] **Step 3: Write the failing smoke test**

Create `frontend/src/App.test.tsx`:

```tsx
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import App from './App';

describe('App', () => {
  it('renders the CodeHaus heading', () => {
    render(<App />);
    expect(screen.getByRole('heading', { name: 'CodeHaus' })).toBeInTheDocument();
  });
});
```

- [ ] **Step 4: Create the test setup file**

Create `frontend/src/test/setup.ts`:

```ts
import '@testing-library/jest-dom/vitest';
```

- [ ] **Step 5: Create the Vitest config**

Create `frontend/vitest.config.ts`:

```ts
import { defineConfig, mergeConfig } from 'vite';
import { defineConfig as defineVitestConfig } from 'vitest/config';
import viteConfig from './vite.config';

export default mergeConfig(
  viteConfig,
  defineVitestConfig({
    test: {
      environment: 'jsdom',
      setupFiles: ['./src/test/setup.ts'],
      globals: true,
    },
  }),
);
```

- [ ] **Step 6: Add the test script**

In `frontend/package.json`, inside `"scripts"`, add:

```json
"test": "vitest run"
```

- [ ] **Step 7: Run the test and verify it passes**

Run:

```
cd frontend && npm test
```

Expected: `1 passed`, exit code 0. (This is a smoke test confirming the test pipeline itself works, so it is written and passing in the same step rather than red-green — there is no production behavior yet to drive with a failing test.)

---

### Task 7: Scaffold the Full Folder Structure

**Files:**

- Create: `frontend/src/app/`, `frontend/src/app/providers/`, `frontend/src/app/router/`
- Create: `frontend/src/modules/{auth,dashboard-client,dashboard-admin,projects,clients,quotations,invoices,payments,messaging,notifications,reports,settings,support}/`
- Create: `frontend/src/shared/components/ui/`, `frontend/src/shared/components/common/`, `frontend/src/shared/components/feature/`, `frontend/src/shared/layouts/`, `frontend/src/shared/hooks/`, `frontend/src/shared/api/`, `frontend/src/shared/store/`, `frontend/src/shared/utils/`, `frontend/src/shared/constants/`, `frontend/src/shared/types/`, `frontend/src/shared/config/`
- Create: `frontend/src/styles/`, `frontend/src/assets/`
- Move: `frontend/src/App.tsx`, `frontend/src/App.test.tsx`, `frontend/src/main.tsx` → `frontend/src/app/`
- Modify: `frontend/src/main.tsx` (update `App` import path after move) and `frontend/index.html` (none expected, verify only)

**Interfaces:**

- Produces: the exact folder layout from the architecture spec Section 1, matching what every subsequent plan (Phases 2-11) will populate. Existing shadcn output (`frontend/src/components/ui/`, `frontend/src/lib/utils.ts`) stays where the CLI put it — do not move CLI-managed files, since re-running `shadcn add` later expects them there. `shared/components/ui/` starts as an empty placeholder for hand-written wrappers, distinct from the CLI's own `components/ui/`.

- [ ] **Step 1: Create all module and shared subfolders with `.gitkeep` placeholders**

Run (PowerShell):

```
$dirs = @(
  'src/app/providers','src/app/router',
  'src/modules/auth','src/modules/dashboard-client','src/modules/dashboard-admin',
  'src/modules/projects','src/modules/clients','src/modules/quotations','src/modules/invoices',
  'src/modules/payments','src/modules/messaging','src/modules/notifications','src/modules/reports',
  'src/modules/settings','src/modules/support',
  'src/shared/components/ui','src/shared/components/common','src/shared/components/feature',
  'src/shared/layouts','src/shared/hooks','src/shared/api','src/shared/store','src/shared/utils',
  'src/shared/constants','src/shared/types','src/shared/config',
  'src/styles','src/assets'
)
foreach ($d in $dirs) {
  $path = "frontend/$d"
  New-Item -ItemType Directory -Force -Path $path | Out-Null
  New-Item -ItemType File -Force -Path "$path/.gitkeep" | Out-Null
}
```

Expected: exits with no error; every listed folder exists under `frontend/src/` with a `.gitkeep` file.

- [ ] **Step 2: Move App.tsx, App.test.tsx, and main.tsx into `app/`**

Run:

```
Move-Item frontend/src/App.tsx frontend/src/app/App.tsx
Move-Item frontend/src/App.test.tsx frontend/src/app/App.test.tsx
Move-Item frontend/src/main.tsx frontend/src/app/main.tsx
```

- [ ] **Step 3: Update the import path in main.tsx**

Open `frontend/src/app/main.tsx`. It currently imports `App` and `index.css` with paths relative to the old `src/` location (`./App`, `./index.css`). Update to:

```tsx
import './index.css';
```

becomes (relative to the new `app/` folder, `index.css` still lives in `src/`):

```tsx
import '../index.css';
```

And the `App` import stays `./App` (both files now live in `app/` together).

- [ ] **Step 4: Update index.html's script entry point if needed**

Open `frontend/index.html`. Confirm the `<script type="module" src="/src/main.tsx"></script>` tag still resolves — Vite resolves this by the file's new path `/src/app/main.tsx`. Update the `src` attribute to:

```html
<script type="module" src="/src/app/main.tsx"></script>
```

- [ ] **Step 5: Verify dev server, build, and tests all still work after the move**

Run:

```
cd frontend && npm run build
```

Expected: exits 0.

Run:

```
cd frontend && npm test
```

Expected: `1 passed` (the smoke test from Task 6, now resolving `./App` from within `app/`).

---

### Task 8: Final Verification

**Files:** none (verification only)

**Interfaces:** none

- [ ] **Step 1: Run the full check sequence**

Run each in `frontend/`, in order, confirming each exits 0 before running the next:

```
npm run lint
npm run format:check
npm run build
npm test
```

- [ ] **Step 2: Confirm the folder structure matches the spec**

Run:

```
cd frontend && Get-ChildItem -Directory -Recurse src | Where-Object { $_.FullName -notmatch 'node_modules' } | Select-Object FullName
```

Expected: output includes every folder listed in Task 7 Step 1, plus the CLI-managed `src/components/ui/` and `src/lib/` from Task 4.

- [ ] **Step 3: Mark Phase 1 complete**

No further action — Phase 1 (Project Setup) is done. Phase 2 (Design System) is a separate plan per the roadmap in the architecture spec.
